# API Base URL - the backend API server
# For local development: http://localhost:3001
# For EC2 via SSH tunnel: http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001

# Data mode: 'mock' (default) or 'http' (connect to real API)
# - mock: Uses in-memory MockDataClient with localStorage persistence
# - http: Uses HttpDataClient to connect to the real backend API
VITE_DATA_MODE=mock

# Feature flags (optional)
VITE_ENABLE_ANALYTICS=false
